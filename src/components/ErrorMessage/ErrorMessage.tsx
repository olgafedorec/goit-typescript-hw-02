import { Toaster } from 'react-hot-toast';

export default function ErrorMessage() {
    return (
        <div>
           <Toaster
  position="top-center"
  reverseOrder={false}
/> 
        </div>
    )
}